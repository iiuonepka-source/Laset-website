# 🧪 Beta Page - Документация

## Описание

Эксклюзивная страница для бета-тестеров с доступом к тестовым билдам LASTED.

## Доступ

### Кто может видеть страницу:
- ✅ Пользователи с ролью **beta**
- ✅ Пользователи с ролью **admin**
- ❌ Все остальные видят сообщение "Доступ ограничен"

### Как появляется вкладка:
- Вкладка "🧪 Beta" появляется в навигации автоматически после входа
- Показывается только для beta и admin пользователей
- Скрыта для всех остальных

## Структура страницы

### 1. Приветственный блок
```
🚀 Добро пожаловать, бета-тестер!
Здесь ты можешь скачать последние тестовые версии LASTED
```

**Особенности:**
- Оранжевая обводка (цвет beta роли)
- Анимированная иконка ракеты
- Backdrop blur эффект

### 2. Список билдов

#### Release Build (текущий)
```
🚀 Release Build
v1.0.0-beta
[Release badge]

Стабильная версия для тестирования

✓ Все основные модули
✓ FunTime bypass
✓ Оптимизация

[Установить 📥]
```

**Особенности:**
- Оранжевая обводка и badge
- Иконка ракеты 🚀
- Кнопка "Установить" с иконкой 📥
- При клике показывает "Coming Soon" модалку

#### Coming Soon блок
```
⏳ Ждите скоро...
Новые билды появятся в ближайшее время!
```

**Особенности:**
- Фиолетовая обводка
- Анимированная иконка ⏳
- Минималистичный дизайн

### 3. Блок "Доступ ограничен"
Показывается если:
- Пользователь не залогинен
- Роль пользователя не beta и не admin

```
🔒 Доступ ограничен
Эта страница доступна только для пользователей с ролью Beta
[На главную]
```

## Технические детали

### Проверка доступа
```javascript
function checkBetaAccess() {
    const saved = localStorage.getItem('laset_user');
    if (saved) {
        const user = JSON.parse(saved);
        if (user.role === 'beta' || user.role === 'admin') {
            // Показать контент
            document.getElementById('betaAccessDenied').classList.add('hidden');
            document.getElementById('betaContent').classList.remove('hidden');
        } else {
            // Показать блокировку
            document.getElementById('betaAccessDenied').classList.remove('hidden');
            document.getElementById('betaContent').classList.add('hidden');
        }
    } else {
        // Не залогинен - показать блокировку
        document.getElementById('betaAccessDenied').classList.remove('hidden');
        document.getElementById('betaContent').classList.add('hidden');
    }
}
```

### Показ вкладки в навигации
```javascript
function updateAccountBtn() {
    const betaLink = document.getElementById('betaLink');
    
    if (currentUser) {
        // Show beta link for beta users and admins
        if (betaLink && (currentUser.role === 'beta' || currentUser.role === 'admin')) {
            betaLink.style.display = 'block';
        }
    } else {
        // Hide beta link
        if (betaLink) {
            betaLink.style.display = 'none';
        }
    }
}
```

## Стили

### Цветовая схема
- **Beta обводка:** `rgba(255, 107, 53, 0.3)` (оранжевый)
- **Beta badge:** `#ff6b35` на `rgba(255, 107, 53, 0.2)`
- **Фон карточек:** `var(--bg-card)` с `blur(20px)`

### Анимации
- **float:** Плавное движение вверх-вниз (3 секунды)
- **hover:** Появление белой обводки + translateY(-4px)

### Адаптивность
- Полностью адаптивный дизайн
- Работает на всех устройствах
- Мобильная версия оптимизирована

## Будущие билды

### Как добавить новый билд:
```html
<div class="build-card">
    <div class="build-header">
        <div class="build-info">
            <div class="build-icon">🚀</div>
            <div>
                <h3>Build Name</h3>
                <span class="build-version">v1.1.0-beta</span>
            </div>
        </div>
        <span class="build-badge dev">Dev</span>
    </div>
    <p class="build-desc">Описание билда</p>
    <div class="build-features">
        <div class="build-feature">✓ Новая фича 1</div>
        <div class="build-feature">✓ Новая фича 2</div>
    </div>
    <button class="btn primary full" onclick="downloadBuild('build-id')">
        <span>Установить</span>
        <span class="btn-icon">📥</span>
    </button>
</div>
```

### Типы badge:
- **release** - Стабильная версия (оранжевый)
- **dev** - Разработка (синий)
- **experimental** - Экспериментальная (красный)

## API Integration (будущее)

### Endpoint для получения билдов:
```javascript
GET /api/beta/builds
Authorization: Bearer {token}

Response:
{
  "builds": [
    {
      "id": "release-1.0.0",
      "name": "Release Build",
      "version": "v1.0.0-beta",
      "type": "release",
      "description": "Стабильная версия",
      "features": ["Feature 1", "Feature 2"],
      "downloadUrl": "https://...",
      "createdAt": "2025-01-27T12:00:00Z"
    }
  ]
}
```

### Endpoint для скачивания:
```javascript
GET /api/beta/download/{buildId}
Authorization: Bearer {token}

Response: Binary file or redirect to download URL
```

## Безопасность

### Проверки:
- ✅ Роль проверяется на клиенте (localStorage)
- ✅ Роль должна проверяться на сервере при скачивании
- ✅ Токен авторизации для API запросов
- ✅ HWID проверка при запуске клиента

### Рекомендации:
- Добавить серверную проверку роли
- Логировать все скачивания билдов
- Ограничить количество скачиваний
- Добавить watermark с UID в билды

## Использование

### Для бета-тестеров:
1. Получить роль Beta от администратора
2. Войти в аккаунт
3. Перейти на вкладку "🧪 Beta"
4. Скачать нужный билд
5. Установить и протестировать
6. Сообщить о багах в Discord/Telegram

### Для администраторов:
1. Выдать роль Beta через админ-панель
2. Загрузить новый билд (когда будет API)
3. Мониторить скачивания
4. Собирать фидбек от тестеров

## Иконки

### Используемые эмодзи:
- 🚀 - Ракета (билды, релизы)
- 🧪 - Колба (beta, тестирование)
- ⏳ - Песочные часы (ожидание)
- 🔒 - Замок (доступ закрыт)
- 📥 - Скачивание
- ✓ - Галочка (готовые фичи)

---

© 2025 LASTED
