# Быстрый запуск Laset API

## Вариант 1: Простой запуск (JSON база)

### Шаг 1: Установка Node.js
Если еще не установлен, скачай с https://nodejs.org/

### Шаг 2: Установка зависимостей
```bash
cd website/api
npm install express cors
```

### Шаг 3: Запуск сервера

**Windows:**
```bash
start.bat
```

**Или вручную:**
```bash
node server.js
```

### Шаг 4: Проверка
Открой браузер: http://localhost:3000

Готово! Сервер работает с JSON базой данных.

---

## Вариант 2: С PostgreSQL (рекомендуется)

Если хочешь полноценную базу данных, следуй инструкциям в `README.md`

---

## Проблемы?

### "Cannot find module 'express'"
```bash
npm install
```

### "Port 3000 already in use"
Измени порт в server.js или останови другой процесс:
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID <номер> /F
```

### "EACCES: permission denied"
Запусти от администратора или используй другой порт

---

## Тестирование API

### Регистрация
```bash
curl -X POST http://localhost:3000/api/register \
  -H "Content-Type: application/json" \
  -d "{\"email\":\"test@test.com\",\"nickname\":\"TestUser\",\"password\":\"123456\"}"
```

### Вход
```bash
curl -X POST http://localhost:3000/api/login \
  -H "Content-Type: application/json" \
  -d "{\"uid\":\"1\",\"password\":\"123456\"}"
```

### Получить следующий UID
```bash
curl http://localhost:3000/api/next-uid
```

---

## Файлы

- `server.js` - Основной сервер (JSON база)
- `server-postgres.js` - Сервер с PostgreSQL
- `users.json` - База данных (создается автоматически)
- `package.json` - Зависимости для PostgreSQL
- `package-simple.json` - Минимальные зависимости

---

## Что дальше?

1. ✅ Сервер запущен
2. 🌐 Открой сайт: http://localhost:3000
3. 📝 Зарегистрируйся
4. 🎮 Скачай клиент

Для продакшена рекомендуется:
- Использовать PostgreSQL (см. README.md)
- Настроить HTTPS
- Использовать reverse proxy (nginx)
- Настроить автозапуск (pm2, systemd)
