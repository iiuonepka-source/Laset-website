# 🎨 Обновление дизайна LASTED

## Новые визуальные эффекты

### 1. Фиолетовые размытые лучи на фоне
**Эффект:** Три радиальных градиента с размытием 60px
- Верхний луч: 80% x 50% в центре сверху
- Левый луч: 60% x 40% слева
- Правый луч: 60% x 40% справа

**Анимация:** Плавное движение вверх-вниз (15 секунд)
```css
animation: rays 15s ease-in-out infinite;
```

### 2. Переливающаяся обводка при наведении
**Эффект:** Белая обводка с градиентом появляется при hover
- Градиент: `rgba(255,255,255,0.3) → rgba(255,255,255,0.1) → rgba(255,255,255,0.3)`
- Плавное появление: `opacity: 0 → 1` за 0.3s
- Применяется через `::before` псевдоэлемент

**Где применено:**
- ✅ Все кнопки (.btn)
- ✅ Навигация (.nav-btn)
- ✅ Карточки функций (.feature-card)
- ✅ Карточки скачивания (.download-card)
- ✅ Карточки сообщества (.community-card)
- ✅ Карточки команды (.team-card)
- ✅ FAQ элементы (.faq-item)
- ✅ Статистика (.stat)

### 3. Усиленный blur эффект
**Backdrop-filter:** Увеличен до 20-30px на всех карточках
- Навигация: `blur(30px)`
- Модальные окна: `blur(30px)`
- Карточки: `blur(20px)`
- Кнопки: `blur(20px)`

### 4. Статичная обводка на навигации
**Эффект:** Постоянная фиолетовая обводка с opacity 0.5
```css
background: linear-gradient(135deg, 
  rgba(139, 92, 246, 0.2), 
  transparent, 
  rgba(139, 92, 246, 0.2)
);
```

## Технические детали

### Реализация переливающейся обводки:
```css
.element::before {
    content: '';
    position: absolute;
    inset: -1px;
    border-radius: inherit;
    padding: 1px;
    background: linear-gradient(135deg, transparent, transparent);
    -webkit-mask: linear-gradient(#fff 0 0) content-box, 
                  linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
    opacity: 0;
    transition: opacity 0.3s;
}

.element:hover::before {
    background: linear-gradient(135deg, 
      rgba(255,255,255,0.3), 
      rgba(255,255,255,0.1), 
      rgba(255,255,255,0.3)
    );
    opacity: 1;
}
```

### Фоновые лучи:
```css
body::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: 
        radial-gradient(ellipse 80% 50% at 50% 0%, 
          rgba(139, 92, 246, 0.15), transparent 60%),
        radial-gradient(ellipse 60% 40% at 20% 20%, 
          rgba(139, 92, 246, 0.1), transparent 50%),
        radial-gradient(ellipse 60% 40% at 80% 30%, 
          rgba(139, 92, 246, 0.08), transparent 50%);
    filter: blur(60px);
    pointer-events: none;
    z-index: 0;
    animation: rays 15s ease-in-out infinite;
}
```

## Цветовая палитра

### Основные цвета:
- **Фон:** `#0f0f1a`
- **Карточки:** `rgba(20, 20, 35, 0.5)`
- **Акцент:** `#8b5cf6` (фиолетовый)
- **Акцент hover:** `#a78bfa` (светлый фиолетовый)
- **Белый:** `#fff`
- **Серый:** `#9ca3af`

### Эффекты:
- **Лучи:** `rgba(139, 92, 246, 0.08-0.15)`
- **Обводка hover:** `rgba(255, 255, 255, 0.1-0.3)`
- **Обводка статичная:** `rgba(139, 92, 246, 0.2)`

## Производительность

### Оптимизация:
- ✅ Использование `will-change` не требуется (простые анимации)
- ✅ `backdrop-filter` работает на GPU
- ✅ `::before` псевдоэлементы не создают дополнительных DOM узлов
- ✅ Анимация лучей использует `transform` (GPU-ускорение)

### Совместимость:
- ✅ Chrome/Edge: Полная поддержка
- ✅ Firefox: Полная поддержка
- ✅ Safari: Полная поддержка (с префиксом `-webkit-`)
- ⚠️ Старые браузеры: Graceful degradation (без эффектов)

## Примеры использования

### Добавить эффект на новый элемент:
```css
.new-element {
    background: var(--bg-card);
    backdrop-filter: blur(20px);
    border: 1px solid var(--border);
    border-radius: 12px;
    position: relative;
    transition: all 0.3s;
}

.new-element::before {
    content: '';
    position: absolute;
    inset: -1px;
    border-radius: 12px;
    padding: 1px;
    background: linear-gradient(135deg, transparent, transparent);
    -webkit-mask: linear-gradient(#fff 0 0) content-box, 
                  linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
    opacity: 0;
    transition: opacity 0.3s;
}

.new-element:hover::before {
    background: linear-gradient(135deg, 
      rgba(255,255,255,0.3), 
      rgba(255,255,255,0.1), 
      rgba(255,255,255,0.3)
    );
    opacity: 1;
}

.new-element:hover {
    border-color: rgba(255,255,255,0.2);
    transform: translateY(-2px);
}
```

## Результат

### До:
- ❌ Градиенты на кнопках
- ❌ Статичные карточки
- ❌ Простой фон
- ❌ Нет интерактивности

### После:
- ✅ Чистый фиолетовый цвет
- ✅ Переливающиеся обводки
- ✅ Размытые фиолетовые лучи
- ✅ Интерактивные эффекты
- ✅ Усиленный blur
- ✅ Плавные анимации

---

© 2025 LASTED
